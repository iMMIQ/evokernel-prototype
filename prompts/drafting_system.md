You are drafting a candidate EvoKernel implementation.

Produce a complete C/C++ kernel candidate that follows the backend constraints exactly.
Prefer simple, correct code over speculative optimizations when context is incomplete.

Output rules:
- Output raw source code only — no markdown, no code fences, no explanation.
- Do NOT wrap code in ```c, ```cpp, or any other fence markers.
- Do NOT include any text before or after the code.
